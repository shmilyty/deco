{
  "name": "deco-tree",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:server": "cd server && node index.js",
    "dev:migrate": "cd server && npx prisma migrate dev",
    "dev:generate": "cd server && npx prisma generate",
    "dev:setup": "cd server && npm install && npx prisma generate && npx prisma migrate dev",
    "build": "vite build",
    "preview": "vite preview",
    "test:root-layout": "node scripts/check-root-layout.mjs"
  },
  "dependencies": {
    "vue": "^3.5.24",
    "vue-router": "^4.6.3"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.1",
    "vite": "^7.2.4"
  }
}
